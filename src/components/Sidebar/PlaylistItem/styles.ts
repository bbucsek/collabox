import styled from 'styled-components'

export const Container = styled.div`
    cursor: pointer;
    margin-top: 1rem;
    color: #003f5a;
    font-weight: 600;
    font-size: 1.2rem;
    transition: 0.2s ease-in-out;

    &:hover {
        transform: scale(1.1)
    }

`